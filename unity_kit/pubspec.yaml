name: unity_kit
description: Flutter plugin for Unity 3D integration — typed bridge, lifecycle management, and communication layer.
version: 0.9.2

homepage: https://github.com/erykkruk/flutter_unity_kit
repository: https://github.com/erykkruk/flutter_unity_kit

topics:
  - unity
  - flutter-unity
  - game
  - assets
  - bridge

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_plugin_android_lifecycle: ^2.0.0
  http: ^1.2.0
  path_provider: ^2.1.1
  crypto: ^3.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  mocktail: ^1.0.0
  fake_async: ^1.3.3

flutter:
  plugin:
    platforms:
      android:
        package: com.unity_kit
        pluginClass: UnityKitPlugin
      ios:
        pluginClass: SwiftUnityKitPlugin
